# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Keep the pinned upstream GodotGAS source byte-for-byte, including its existing whitespace.
addons/GodotGAS/** whitespace=-trailing-space
